# Deploy static build (static-app/) to GitHub Pages.
#
# - Direct pushes to main or master deploy.
# - Merging a PR into main or master also creates a push to that branch, so merges
#   deploy automatically without a separate pull_request workflow (which would double-run).
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - master

concurrency:
  group: pages
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: npm
          cache-dependency-path: |
            core/package-lock.json
            app/package-lock.json

      - name: Install and build (core + app → static-app)
        run: npm run build:all

      - uses: actions/upload-pages-artifact@v3
        with:
          path: static-app

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
